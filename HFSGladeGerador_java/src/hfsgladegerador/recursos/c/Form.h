#ifndef <classeForm>_H
#define <classeForm>_H
// ---------------------------------------------------------------------------
#include "resource.h"
// ---------------------------------------------------------------------------
struct S<classeForm> {
	<declaracaoObjetos>
	GtkWidget *<objForm>;
};
struct S<classeForm> <classeForm>;
// ---------------------------------------------------------------------------
	GtkWidget *<classeForm>_Criar();
	void <classeForm>_Mostrar();
	void on_<classeForm>_destroy(GtkObject *object, gpointer user_data);

	<declaracaoMetodos>
// ---------------------------------------------------------------------------
#endif
// ---------------------------------------------------------------------------
